<h1>Listing additions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Amount</th>
    <th>Description</th>
    <th>Budget</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @additions.each do |addition| %>
  <tr>
    <td><%= addition.name %></td>
    <td><%= addition.amount %></td>
    <td><%= addition.description %></td>
    <td><%= addition.budget_id %></td>
    <td><%= addition.user_id %></td>
    <td><%= link_to 'Show', addition %></td>
    <td><%= link_to 'Edit', edit_addition_path(addition) %></td>
    <td><%= link_to 'Destroy', addition, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Addition', new_addition_path %>
